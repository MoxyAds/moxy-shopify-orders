# ───── Node / PNPM / Yarn / NPM ─────
node_modules/
.pnpm/
.jspm/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ───── Remix build artefacts ─────
.build/          # Remix’s production build (if you ever run `remix build`)
public/build/    # Vite-emitted client bundles (hash-named)

# ───── Vite / esbuild cache ─────
.vite/
dist/
.cache/

# ───── Shopify CLI ─────
.shopify/        # tunnel certs, cached metadata
.env             # API keys, DB urls, etc.
*.pem            # mkcert or Cloudflare certs that land in the repo root

# ───── Databases & prisma artefacts (if you use SQLite locally) ─────
*.sqlite
*.sqlite-journal
prisma/migrations/

# ───── Testing / coverage ─────
coverage/
*.lcov

# ───── OS / editor junk ─────
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.sw?

# ───── optional: macOS/Linux files you never want to commit ─────
*.orig
